import React from "react";

import { Layout } from "components/Layout";
import { Theme } from "components/Theme";

export const App = () => {
  return (
    <Theme>
      <Layout />
    </Theme>
  );
};
