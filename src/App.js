import React from "react";

import { Header } from "components/Header";
import { Theme } from "components/Theme";

export const App = () => {
  return (
    <Theme>
      <Header />
    </Theme>
  );
};
